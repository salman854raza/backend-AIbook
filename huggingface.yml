# Hugging Face Space Configuration
version: "1.0"
services:
  rag-chatbot:
    build: .
    ports:
      - "7860:7860"
    environment:
      - PORT=7860
    secrets:
      - COHERE_API_KEY
      - OPENROUTER_API_KEY
      - QDRANT_URL
      - QDRANT_API_KEY
      - DOCUSAURUS_URL